# Agent Memory Lab — Python dependencies
# Iteration 1: the agent + a test runner. Memory deps (bedrock-agentcore) arrive
# in Iteration 3. Pin loosely for a learning lab; tighten if results need it.

strands-agents>=0.1.0      # the agent SDK
boto3>=1.34.0              # AWS SDK (Bedrock); usually pulled in transitively too
pytest>=8.0.0             # smoke + future tests
