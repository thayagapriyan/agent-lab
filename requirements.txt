# Agent Memory Lab — Python dependencies
# Pin loosely for a learning lab; tighten if results need it.

strands-agents>=1.44.0       # the agent SDK (Agent, BedrockModel, session_manager)
bedrock-agentcore>=1.15.0    # AgentCore Memory session manager + config (Iteration 3)
boto3>=1.34.0                # AWS SDK (Bedrock); usually pulled in transitively too
python-dotenv>=1.0.0         # auto-load a local .env for convenience (optional)
pytest>=8.0.0                # smoke + memory tests
