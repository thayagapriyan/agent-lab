# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets / local config — never commit
.env
.env.local
*.env
!.env.example

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
# Keep the lock file tracked (Terraform best practice — pins provider versions).
# (.terraform.lock.hcl is intentionally NOT ignored.)

# Sweep outputs (large/regenerable); results are recorded in AGENTS.md
*.csv

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
